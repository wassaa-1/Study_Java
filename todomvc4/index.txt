step03(todomvc)에서의 한계점
1. 데이터를 파싱하는 부분을 별도의 클래스로 분리하였지만, 여전히 메인화면에 해당하는 App.java에서 View(조회 부분), 연산 처리 등을 전부 수행하고 있음
-> GodClass

Step04
1. Todo 등록 기능(파일 쓰기)을 별도의 클래스 TodoTSVWriter로 작성
2. 배열(Todo[])을 컬렉션으로 변경

개선된 부분
1. 배열이 컬렉션으로 변경되면서 배열의 길이를 고려할 필요가 없어짐

한계점
조회, 등록 등의 내용이 App.java 내부에서 한 번에 이루어지고 있기 때문에 별도의 View, Controller로 분리 필요